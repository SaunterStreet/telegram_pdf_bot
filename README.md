# 🤖 Telegram PDF Bot

<div align="center">

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![Docker](https://img.shields.io/badge/Docker-Ready-brightgreen.svg)](https://www.docker.com/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

*Бот, который собирает данные пользователя и возвращает готовый PDF прямо в Telegram.*

[Возможности](#-возможности) • [Быстрый старт](#-быстрый-старт) • [Конфигурация](#️-конфигурация) • [Использование](#-использование)

</div>

---

## ✨ Возможности

- 📄 Генерация PDF по готовым шаблонам (Israel, Ireland, UK)
- 💬 Нативная интеграция с Telegram через Bot API
- 🧠 Хранение контекста: бот понимает, какой документ от пользователя ждёт
- 🐳 Docker-образ для быстрого деплоя
- 🔐 Все файлы генерируются локально и тут же удаляются

## 🚀 Быстрый старт

```bash
git clone https://github.com/your-username/telegram_pdf_bot.git
cd telegram_pdf_bot

cp .env.example .env
nano .env  # Вставьте токен, полученный у @BotFather

docker-compose up -d
```

Бот начнёт работу и подключится к Telegram сразу после сборки контейнера.

## 🛠️ Локальная установка

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

## ⚙️ Конфигурация

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен бота, выданный @BotFather | `123456:ABC-DEF` |

> Для обратной совместимости можно использовать переменную `BOT_TOKEN`, если вы не хотите переименовывать существующий `.env`.

## 📱 Использование

1. Напишите боту в Telegram и выполните `/start`.
2. Выберите нужный шаблон: `/israel`, `/ireland` или `/uk`.
3. Следуйте подсказке и отправьте данные строго отдельными строками.
4. Получите PDF документ, который бот отправит этим же чатом.
5. Команда `/return` отменяет текущий режим, `/info` показывает справку.

### Команды бота

| Команда | Назначение |
|---------|------------|
| `/start` | Краткое описание и подсказки |
| `/info` | Памятка с описанием режимов и количеством строк |
| `/israel` | Включает режим счёта Israel (20 строк) |
| `/ireland` | Включает режим Ireland (23 строки) |
| `/uk` | Включает режим UK (12 строк) |
| `/return` | Сбрасывает активный режим |

## 🐳 Docker

```bash
docker-compose up -d        # запуск
docker-compose logs -f      # просмотр логов
docker-compose restart      # перезапуск
docker-compose down         # остановка
docker-compose up -d --build  # пересборка
```

## 📁 Структура

```
telegram_pdf_bot/
├── main.py                # Telegram-бот
├── pdf.py                 # Работа с PDF и генерация текста
├── templates.py           # Подсказки для пользователя
├── template_*.pdf         # Базовые шаблоны
├── fonts/                 # Шрифты, нужные ReportLab
├── requirements.txt       # Зависимости
├── docker-compose.yml     # Docker-оркестрация
└── Dockerfile             # Описание контейнера
```

## 🧩 Разработка

- Проект написан на Python 3.11 и использует `python-telegram-bot`.
- Тестировать генерацию PDF можно локально через `test.py`.
- Перед деплоем не забывайте перегенерировать образ или обновить зависимости.

Удачи в автоматизации! 🚀

